# Compatibility override for installations that used an older Compose file.
# New installations already attach the dashboard to the shared media network.
services:
  dashboard:
    networks:
      - dashboard
      - media-net

networks:
  media-net:
    external: true
    name: ${MEDIA_NETWORK:-media-net}
